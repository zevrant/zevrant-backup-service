server:
  ssl:
    key-store: /opt/acme/certs/zevrant-services.p12
    key-store-type: PKCS12

spring:
  datasource:
    url: 'jdbc:postgresql://zevrant-backup-service-db:5432/backup'
    username: 'zevrant'
    password: ${rds.backup.password}
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: https://develop.zevrant-services.com/auth/realms/zevrant-services-develop

logging:
  level:
    com.zevrant.services: debug
    com.zevrant.services.zevrantbackupservice.services: debug
    root: info

zevrant:
  keystore:
    password: /var/zevrant-services/keystore/password
  vault:
    username: /var/zevrant-services/vault/username
    password: /var/zevrant-services/vault/password